# Placeholder controller parameters for Murphy (ros2_control).
# Extend with joint_trajectory_controller / forward_command_controller as hardware stabilizes.

controller_manager:
  ros__parameters:
    update_rate: 250  # Hz — tune for Pi5 + CAN budget

murphy_joint_state_broadcaster:
  ros__parameters: {}
