<?xml version="1.0"?>
<launch>
  <!-- Rudy ROS 2: prefer XML launch; params in YAML where practical. -->
  <arg name="use_sim_time" default="false"/>
  <arg name="rviz_config" default="$(find-pkg-share bringup)/rviz/display.rviz"/>

  <set_use_sim_time value="$(var use_sim_time)"/>

  <let name="robot_description"
    value="$(command 'xacro $(find-pkg-share description)/urdf/robot.urdf.xacro')"/>

  <node pkg="robot_state_publisher" exec="robot_state_publisher" output="screen">
    <param name="robot_description" value="$(var robot_description)"/>
  </node>

  <node pkg="joint_state_publisher_gui" exec="joint_state_publisher_gui" output="screen"/>

  <node pkg="rviz2" exec="rviz2" output="screen" args="-d $(var rviz_config)"/>
</launch>
